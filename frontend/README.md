# Frontend

This is the React frontend for the Smart Budget E-Commerce System.

## Setup

```bash
cd frontend
npm install
npm start
```

The frontend will run on http://localhost:3000 and expects the backend to be available at http://localhost:8000.
